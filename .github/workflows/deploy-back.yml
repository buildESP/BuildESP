name: Deploy Backend

on:
  push:
    branches:
      - DevOps_ESP-51  # Déclenche le workflow quand on push sur cette branche
    paths:
      - 'backend/**'  # Ne déploie que si le backend est modifié

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_BACK_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy Backend to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.MY_BACK_SERVER }}@${{ secrets.EC2_BACK_HOST }} << 'EOF'
            echo "🚀 Déploiement en cours sur le serveur..."

            cd /opt/bitnami/apps/backend  # Assurez-vous que ce chemin est correct
            echo "📥 Pulling latest changes..."
            git pull origin DevOps_ESP-51  # Récupère les nouvelles modifications

            echo "📦 Installing dependencies..."
            npm install  # Installe les dépendances

            echo "🚀 Vérification de PM2..."
            if ! command -v pm2 &> /dev/null; then
              echo "⚠️  PM2 non trouvé. Installation en cours..."
              npm install -g pm2
            fi

            echo "🔄 Restarting backend app..."
            pm2 restart backend || pm2 start server.js --name "backend"  # Redémarre ou démarre le serveur

            echo "✅ Déploiement terminé avec succès !"
          EOF
