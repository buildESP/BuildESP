name: Deploy Backend

on:
  push:
    branches:
      - DevOps_ESP-51  # Déclenche le workflow quand on push sur cette branche
    paths:
      - 'backend/**'  # Ne déploie que si le backend est modifié

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          # Crée le répertoire .ssh
          mkdir -p ~/.ssh
          
          # Ajoute la clé privée SSH à partir du secret
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          
          # Change les permissions de la clé pour qu'elle soit sécurisée
          chmod 600 ~/.ssh/id_rsa
          
          # Ajouter l'hôte à known_hosts pour éviter les questions interactives sur l'authentification de l'hôte
          ssh-keyscan -H ${{ secrets.EC2_BACK_HOST }} >> ~/.ssh/known_hosts

      - name: Test SSH Connection
        run: |
          # Testez la connexion SSH pour voir si ça fonctionne bien
          ssh -v -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ubuntu@${{ secrets.EC2_BACK_HOST }} "echo 'Connexion SSH réussie!'"

      - name: Deploy Backend to EC2
        run: |
          # Effectue le déploiement
          ssh -t -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ubuntu@${{ secrets.EC2_BACK_HOST }} << 'EOF'
            echo "🚀 Déploiement en cours sur le serveur..."

            # Changez de répertoire vers le bon chemin
            cd /opt/bitnami/apps/backend/backend/api || exit 1
            echo "📥 Pulling latest changes..."
            git pull origin DevOps_ESP-51 || exit 1

            echo "📦 Installing dependencies..."
            npm install || exit 1

            echo "🚀 Vérification de PM2..."
            if ! command -v pm2 &> /dev/null; then
              echo "⚠️  PM2 non trouvé. Installation en cours..."
              npm install -g pm2
            fi

            echo "🔄 Restarting backend app..."
            pm2 restart backend || pm2 start server.js --name "backend"  # Vérifiez que `server.js` existe

            echo "✅ Déploiement terminé avec succès!"
          EOF
