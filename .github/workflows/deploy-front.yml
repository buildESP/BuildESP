name: Deploy Frontend

on:
  push:
    branches:
      - DevOps_ESP-51  # Déclenche l'action sur cette branche
    paths:
      - 'frontend/**'  # Ne déploie que si des fichiers dans frontend/ changent

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          echo "Configuring SSH..."
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_FRONT_HOST }} >> ~/.ssh/known_hosts
          echo "StrictHostKeyChecking=no" >> ~/.ssh/config
          echo "UserKnownHostsFile=/dev/null" >> ~/.ssh/config
          echo "LogLevel=DEBUG" >> ~/.ssh/config
          cat ~/.ssh/config  # Affiche la configuration SSH pour déboguer
          ssh -vvv -o StrictHostKeyChecking=no ${{ secrets.MY_FRONT_SERVER }}@${{ secrets.EC2_FRONT_HOST }} "echo SSH Connection Successful"  # Débogage avec -vvv

      - name: Deploy Frontend to EC2
        run: |
          echo "🚀 Déploiement en cours sur le serveur..."
          
          # Se connecter à l'instance EC2 et exécuter les commandes de déploiement
          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ${{ secrets.MY_FRONT_SERVER }}@${{ secrets.EC2_FRONT_HOST }} << 'EOF'
            echo "🚀 Déploiement en cours sur le serveur..."

            # Naviguer dans le répertoire du projet
            cd /var/www/front
            echo "📥 Pulling latest changes..."
            git pull origin DevOps_ESP-51  # Récupère les nouvelles modifications

            cd frontend/app  # Aller dans le bon dossier
            if [ -f package.json ]; then
              echo "📦 Installing dependencies..."
              npm install  # Installe les dépendances

              echo "⚙️  Building frontend..."
              npm run build  # Compile le projet si nécessaire
            else
              echo "❌ ERREUR: package.json non trouvé dans /var/www/front/frontend/app/"
              exit 1
            fi

            # Vérification et installation de PM2 si nécessaire
            echo "🚀 Vérification de PM2..."
            if ! command -v pm2 &> /dev/null; then
              echo "⚠️  PM2 non trouvé. Installation en cours..."
              npm install -g pm2  # Installer PM2 si nécessaire
            fi

            echo "🔄 Restarting frontend app..."
            pm2 restart front || pm2 start npm --name "front" -- run start  # Démarre ou redémarre l'application
            
            echo "✅ Déploiement terminé avec succès !"
          EOF

